node_modules/
.env
uploads/
*.log
.DS_Store
dist/
reelday/
server-out.txt
server-err.txt
.claude/scheduled_tasks.lock

# Music library — keep manifests + folder structure tracked, ignore the actual
# audio bytes (each MP3 is several MB and would bloat the repo).
music-library/**/*.mp3
music-library/**/*.m4a
music-library/**/*.wav
music-library/**/*.ogg

# Lighthouse reports (run-time artifacts, not source).
reelday.ph_*.report.html
*.lighthouse.html
*.report.html

# Demo/raw video drops — large binaries, kept locally only.
video/
wedding-demo/

# Marketing / Remotion wall-recorder tool — kept LOCAL ONLY, never pushed.
# Per session decision 2026-05-26: marketing source + rendered mp4s stay
# off the remote.
tools/wall-recorder/
